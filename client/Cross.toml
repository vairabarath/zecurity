# protoc is installed on the GitHub Actions runner and volume-mounted into
# the cross container via CROSS_CONTAINER_OPTS — no apt-get needed.
[target.x86_64-unknown-linux-musl]

[target.aarch64-unknown-linux-musl]
